# DynamicReportFormatter
This program demonstrates playbooks framework capabilities - NL-to-Python-to-NL function calls.

```python
@playbook
async def generate_report_summary() -> None:
    # Assume report_data is gathered here by Python logic
    report_data = {"sales": 1000, "region": "North", "trend": "positive"}
    # Python calls the NL playbook 'FormatSummary', passing the dictionary
    summary = await FormatSummary(report_data)
    await Say("user", f"Generated Summary: {summary}")
```

## FormatSummary($report_data:dict) -> str
Generate a brief, engaging summary based on the provided report data
### Steps
- 01:EXE Generate a brief, engaging summary based on the provided $report_data
- 02:RET the summary

## Main() -> None
Main program execution flow
### Triggers
- T1:BGN At the beginning
### Steps
- 01:QUE generate_report_summary()
- 02:YLD for call
- 03:YLD for exit

```public.json
[]
```