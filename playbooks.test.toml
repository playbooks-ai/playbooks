# Test environment overrides
debug = false
artifact_result_threshold = 80  # Lower threshold for testing

[model]
name = "claude-sonnet-4-20250514"

[llm_cache]
type = "disk"
enabled = true
path = ".llm_cache_test"

[langfuse]
enabled = false